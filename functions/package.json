{
  "name": "wiselyrise-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for WiselyRise contact form",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-functions": "^4.0.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "firebase-admin": "^11.0.0"
  }
}
